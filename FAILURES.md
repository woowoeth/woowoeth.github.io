# 踩过的坑，和它们各自变成了什么

学 WikiSkill（[arXiv:2608.27454](https://arxiv.org/abs/2608.27454)）的三层分法：
**原始执行经验 / 沉淀的知识 / 可执行的技能**。这个仓前两层一直有（会话记录、
HANDOVER.md），第三层也有（七道闸），**缺的是中间那道编译** —— 教训写完就躺着，
等下次碰巧读到。而「读到也会忘」这张表自己就是证据。

所以每一条都必须写明去向：**变成了哪道闸，或者为什么变不成**。
写「无」不丢人 —— 最后一条的结论就是「类别判断该用眼睛，找不到指标别硬找」。
这道账由 `scripts/wikigate.py` 强制，它挂在 `scripts/gate.py` 上。

格式：每条一个 `##` 段落，段内必须有一行 `**闸：**`。
点名的脚本必须真的存在、真的挂在 `gate.py` 的 CHECKS 上、且在
`gate_selftest.py` 里有反向注入。写「无」的必须给理由（`无 —— <理由>`）。

---

## 只验「好稿能过」等于没验

站主定的规矩。门禁写完之后我们只喂了合格的稿子，看它放行，就宣布它能用了。
那只证明了它不误伤，没证明它抓得住。真正要问的是：**喂一份该被拦的，它拦不拦。**

**闸：** `scripts/gate_selftest.py` —— 29 条失败分支逐条注入，注入后必须返回 1，
且必须报出这条分支自己的理由。

## 只看退出码会被「撞到别的分支」骗过去

注入一个缺陷，门禁红了，就算这条分支验过了吗？不算。注入可能**碰巧触发了另一条分支**，
目标分支依然是死的，而自检显示绿。

这不是假想。视频侧我注入 `art="artFocus"` 用的是全文件第一处替换，
而 artFocus 在 p21 就出现过一次 —— 注入落到了 p21 上，目标行根本没动，
自检于是误报「这条规则是死的」。反过来也一样会骗人。

**闸：** `scripts/gate_selftest.py` —— 每条用例带一个「必须报出的理由」，
`want not in out` 就判不合格，单独报成「拦下了，但理由不是这一条」。

## Python 的 .pyc 缓存会让门禁读到上一条用例的注入

`gate_selftest` 里多数注入恰好都改动 4 个字节（加一对 `==`）。Python 判断 .pyc
是否新鲜看的是源文件的 **(mtime 秒, size)** —— 两条用例落在同一秒内时，
第二次注入的 (mtime, size) 和第一次完全一致，Python 直接加载上一条用例编译出来的
.pyc。症状是「金句红字」这条报出「分则里有红字」。

**只看退出码的话这一步完全隐形** —— 两条用例都红，两条都会被记成通过。
是判据②把它顶出来的。

**闸：** `scripts/gate_selftest.py` —— `_drop_pycache()`，每次跑门禁前清掉
`seo/` 和 `scripts/` 下的 `__pycache__`。

## 真值守卫会让分支永远不触发

`check_chapters.py` 里原本写的是：

    if c["src"] and not plain(c["src"]).strip():
        bad.append("empty src")

空串是 falsy，所以这条分支**对真正空掉的 src 永远不触发**，只能抓到
「写了但全是 `==` 或空白」这种更罕见的情况 —— 它想拦的那种恰好拦不住。
全站 376 章，写这条的人以为它在守着，其实没有。

同一个仓里还有一次同族的：`check_pullquotes.py` 的文件头自己记着
「脚本末尾根本没有 sys.exit，无论抓到多少都返回 0，等于一直空转」。

**闸：** `scripts/check_chapters.py` 已改成 `if not plain(c.get("src") or "").strip()`；
这一类的通用防线是 `scripts/gate_selftest.py` —— 注入空 src 时它报「这条分支是死的」，
正是这么发现的。

## 会临时改源文件的工具，要假设自己随时被 kill

`finally` 挡不住 SIGKILL，也挡不住 Ctrl-C。自检跑到一半被打断，注入的缺陷就永久留在
源文件里；下一次启动备份的是**已经被注入的**版本，于是「还原」把缺陷又写回去。
视频侧真的发生过：`parts-people.js` 里的 `man` 被改成跟 `me` 一样，外加一个
`_manOld` 残渣，之后 `cast_gate` 报「两个角色像素级完全一样」，看着像画错了。

**闸：** `scripts/gate_selftest.py` —— 备份走固定目录 + 清单，清单先落盘再注入；
启动时 `recover_pending()` 先补上次没还原的；`preflight()` 每次模拟一次
「跑到一半被 kill」，确认残留真的会被还原（自检没法进自己的用例表，这是替代品）。

## 同一台机器上的两个副本会抢同一份备份

上一条的备份目录如果是固定路径 + 按 basename 命名，那么克隆、worktree、
另一个会话同时跑的时候，`bak/curie.py` 会互相覆盖，「还原」能把**另一个仓**的内容
写进来。视频侧就是这么从一个临时 worktree 里把主仓的文件还原了一次。

**闸：** `scripts/gate_selftest.py` —— 备份目录名掺仓根路径的 md5 前 8 位。

## 写死的注入锚点会失效，而「用例失效」比「门禁失效」更难发现

锚点写字面量，稿子一改就失效。失效的表现是自检报「这道门禁不合格」——
看起来像门禁坏了，实际是用例坏了，方向正好反。

**闸：** `scripts/gate_selftest.py` —— 锚点一律从文件当前内容里正则取，
且「挑不到注入点」单独报成「用例失效」，不与「这条分支是死的」混在一起。

## 注入前就不绿 ≠ 这道闸不合格

工作区本来就脏的时候，自检会把「基线不绿」记成「门禁抓不住」，把人引向错误的修复方向。

**闸：** `scripts/gate_selftest.py` —— 每道闸先取一次基线，基线非 0 时单独报
「工作区本来就是脏的（不是这道闸的问题）」。

## 要验的是「你的设计会诱导出来的那种输入」

结尾加了追问，却只测单轮 —— 用户真会顺着追问续聊，而续聊那条路一次都没验过。

**闸：** `tests/convo_multiturn.py`

## 图像判重指标代替不了眼睛

封面「看着重复」这件事，先后换过四个指标：裸剪影 IoU、填实后 IoU、
归一化像素差、等墨量二值化 IoU。四个答案互相矛盾 —— 填实抹掉了区分两张图的那条
金色折线；像素差量的是墨量，把最空的几对排成最像；二值 IoU 量的是线条配准，
把「两张都是有横线的纸」排到最不像的一档。

没有一个在回答读者的问题：「这张我是不是刚见过」。那是个类别判断。
真正的毛病也不在图形本身，是**排期**：同一个形隔一两集就再来一次。

**闸：** 无 —— 类别判断该用眼睛，这一维不需要指标。
真正可闸的是排期那一维（同季内相隔 ≤ 4 篇算撞），已在视频侧
`drafts/series.py` 规则 6 落地，不属于本仓。

## 文档里写着「构建时会校验」，不等于真的会

`scripts/hwx_scenes.py` 的文件头一直写着「所有 (slug, k) 引用在构建时逐条校验，
写错一个就构建失败」。2026-09-01 实测那句话是假的：手写一个不存在的
`("li-kuan-yew-PLACEHOLDER","x")`，七道闸一道都没拦住。坏引用不会让构建报错，
只会让那条问题在页面上和检索里静悄悄地少一个答案 —— 属于最难发现的一类。

**教训不是「文档写错了」，是「文档声称的保障必须有可执行的对应物」。**

**闸：** `scripts/check_coverage.py` 的检查 ⓪（引用必须指向真实存在的章节），
反向注入见 `scripts/gate_selftest.py` 的「覆盖·坏引用」。

## 注入打偏比门禁失效更难发现，因为症状一模一样

建这套自检的过程里，我自己打偏了三次，每次的症状都是「✗ 这条分支是死的」或
「✗ 理由不是这一条」—— 看起来全是门禁的问题，实际全是用例的问题：

1. `art="artFocus"` 用全文件第一处替换，落到了 p21 而不是目标行（视频侧）。
2. 「金句红字」注入正确，但 .pyc 缓存让门禁读到上一条用例的注入。
3. 「缺分享图」删的是**条目页**的 og.png，而那道检查只查**章节级**那一层。
4. 「查资料语气」的测试串同时触发了「无人称无情境」，报出的理由不是目标那条。

四次里只有第 2 次是真 bug（缓存），另外三次都是用例自己的错。
所以「✗」的第一反应应该是**先怀疑用例**，不是先改门禁。

**闸：** `scripts/gate_selftest.py` —— 锚点从内容里取、必须核对指定的理由、
「用例失效」与「分支是死的」分开报。三条都是为了让这四类打偏当场现形。

## 门禁系统本身不在版本控制里，等于没有门禁

视频侧那套十二道门禁，`artgate.py`、`gate_selftest.py`、每一个 gate 脚本，
在 git 里**一个文件都没有**。上一个 commit 的标题写着「门禁加固」。
用 `git worktree add --detach HEAD` 克一份出来实测才发现的 —— 推理是发现不了的。

同一族的更深一层：`series.py` 的「已推锁定」拿 `git show HEAD:drafts/series.py`
当真源，而那个文件不在 git 里，取到空串就静默跳过，二十几篇的配色锁空了几个月。

**闸：** 无 —— 这一条没法靠站内脚本自查（脚本自己也可能不在库里，
那就成了「用没入库的东西检查入库情况」）。可执行的对应物是流程：
新增门禁脚本必须和它的反向注入同一个 commit 进库，
并且用 `git worktree add --detach HEAD` 克一份跑一次全量再宣布完成。

## 验证一个正在被别人改的文件集合，必须前后各算一次哈希

跨会话协作时，另一个会话可能正在改你要提交的文件。做法是：等对方空闲 → 固化组合
md5 → 克一份 HEAD 叠加跑全量 → **回头再算一次 md5**。少了最后这一步，
你验证的是快照，提交的是现状，两者可能不是一回事。

今天就撞上了：对方空闲那一刻固化 `2303e458`，跑完全量回头再算是 `48ae2199`，
逐个比对 12 个文件只有 `parts.js` 变了。而克隆里挂掉的 4 个动作，正是因为
我复制到了中间态 —— **症状看起来像「文件集合缺东西」，实际是「靶子在动」**。
两者的修法完全相反：前者要加文件，后者要停下等。

**闸：** 无 —— 这是跨会话的流程，站内脚本查不到别的会话在干什么。
可执行的对应物：提交并发修改的文件集合时，前后各算一次组合哈希，不一致就不提交。

## OpenCC 的分词会跨词边界切错，光转一遍达不到质量线

简繁转换看着是个「跑一遍库」的活，实测有三层坑：

一、**s2twp 会替换词汇**，在这个仓的语料上有硬伤：「如循环之无端」（孙子兵法原句）
    转成「如迴圈之無端」，被当成编程的 loop；「作者的对象」→「物件」；
    「支持的证据」→「支援」。用 s2tw（只转字形）。

二、**分词跨词边界切错**。OpenCC 的分词词典只收「需要转换的词」，「明白」不在里面，
    于是「明白|发生」被贪心切成「明|白发|生」→「明白髮生了什麼」。同类的还有
    「还是只让我」→「還是隻讓我」（副词「只」被当量词）。官方 C++ 版 1.4.2 也一样，
    是原理性限制。全语料逐字审出 40 处这类错，逐条写进修正表。

三、**占位符不能用 \x00**。为保护 URL 不被转换，第一版拿 \x00 当占位符——
    而 OpenCC 底层是 C 字符串，遇 \x00 直接截断，转出来的页面里
    `href="居里 — 人類世界生存法則"`，URL 被正文填满。页面照样渲染、构建照样通过。
    改用私用区字符 \ue000。

**闸：** `scripts/check_tw.py` —— 六条判据：结构一一对应、链接集合一致（正是这条
抓到 \x00 那个 bug）、无漏转、歧义字三字上下文必须在 `tw_allow.txt` 登记过、
hreflang 两版一致、地图类文件指向自己。反向注入见 `scripts/gate_selftest.py`
的「繁体·」五条。

## 「补内容」不等于「按覆盖率补」

空字段常常是结构上就该空的。按覆盖率补会补出一堆为了填格子而写的东西。

**闸：** 无 —— 这是编辑判断，值不值得写只能人定。
可闸的部分是形制（长度、分则数、三问格式），已在 `scripts/check_chapters.py`。

## 17. 两版一起错，对比两版的检查就看不见

繁体站 555 页的页脚指向 `https://ourword.ai/tw/podcast/` —— 不存在的地址。
原声和品味是各自独立的站，它们的繁体版在自己站内（`/podcast/tw/`），
套不上主站的语言前缀。

真正的问题不是这一处，是**已有的闸门在结构上看不见它**：「繁简两版链接集合
一致」那条把繁体链接去掉 `/tw/` 再和简体比，于是两边一起错就一起放行——
它甚至把后来改对的 `/podcast/tw/` 判成了「对不上」，等于在强制执行这个 bug。

同一族一共挖出六个，全都是「构建成功、页面渲染正常、只有真的点进去才发现」：

1. 姊妹站被套上主站语言前缀 → `/tw/podcast/`（555 页）
2. `build_tw` 把 `en/` 整棵树当简体内容转了一遍，塞出一堆 `tw/en/…` 孤儿页
3. JSON-LD 里的 `"url"`（冒号不是等号）仍指简体页：繁体首页的 ItemList
   一百多条全指向简体，Article.url 和 canonical 互相打架
4. 跳转桩的 `content="0;url=…"` 以数字开头，URLISH 认不出是地址；
   桩里的 `<script>location.replace("…")</script>` 会真的执行——
   繁体读者点分类被弹回简体页
5. 英文页的资源指向 `/en/assets/`，而英文站根本不复制资源 →
   线上会是一页没有样式的裸 HTML
6. `_derived(dp)` 按**子串**判断路径，`"/two-handles"` 里含 `"/tw"`，
   于是 5 个页面被当成语言目录整个跳过，语言层、夜间模式、聊天挂件全没有

**闸：**`scripts/check_links.py`

它不比对两版，只问一件事：这条地址指到的东西存不存在。两边一起错也躲不过去。
外加一条：每个非跳转页都必须带 `<!--HWX:LANG-->`，专防「被 walk 静默跳过」。
已挂进门禁清单。

地址改写规则收进 `scripts/lang_urls.py` 一份，两个构建器共用——
这一段被抄成两份之后，两份一起错了三次。

## 18. 字体下载了，但没有任何规则去用它

英文站的 Google Fonts 链接早就换成了 Newsreader + Source Serif 4，字体
每次都老老实实下载。但那段 `html[lang="en"]` 的变量覆盖**从来没进过仓库**
——我把它写在 `assets/hw-entry.css` 末尾，写了两次，两次都丢（那是中文站的
样式表，谁碰一下、哪次回滚一下，尾巴上这块就没了）。于是整个英文站一直拿
宋体和苹方去渲染拉丁字母：西文字形是配汉字设计的，字宽被压成半角以对齐
字身框，重心偏高，斜体多半是机器倾斜——一行中英混排看不出来，整页英文长文
读起来就是「哪里不对但说不出」。是用户告诉我的，不是闸门。

当时 ①–⑫ 全绿，因为它们只看有没有中文**字**，看不见用什么**字体**去排英文。

**闸：** `scripts/check_en.py` 第 ⑬ 条。判据落在**结果**上：渲染英文页，
读 `getComputedStyle` 解出来的 font-family，出现任何 CJK 字体名就拦。
第一版量的是「源码里有没有那段覆盖」——那是个代理，而代理会和它代表的
东西脱钩，这次就是脱钩了两回。另外那段 CSS 现在由 `scripts/build_en.py`
**生成**成 `assets/hw-en.css`，不再是手写的尾巴。

## 19. `1fr` 的下限是 `auto`，横向滚动条把整页顶开

移动端把侧栏目录改成了一条横向滚动的胶囊带。桌面端一切正常，窄屏下整个
英文条目页横着滚，正文右半边在屏幕外——`.layout{grid-template-columns:1fr}`
里的 `1fr` 等价于 `minmax(auto,1fr)`，而 `auto` 这个下限取的是内容的
min-content；那条胶囊带的 min-content 就是整条带子的宽度，网格轨道被顶到
2000px 开外。同一份 CSS 三种语言共用，英文标题更长，发作更重。

也是用户告诉我的。所有闸门都在桌面尺寸下跑，没有一道量过窄屏。

**闸：** `scripts/check_mobile.py` —— 375px 下量三种语言的首页、条目页、
章节页、全集页，判据是 `documentElement.scrollWidth <= clientWidth + 1`，
也就是「读者不需要横向滚动」。

## 20. 共用挂件的文案挪出页面之后，繁体转换就够不着了

聊天挂件那 32 条文案原来内联在每一页里，繁体构建顺带就转了。我把它们
挪进 `assets/hw-chat.js` 一张表里——那是 `/assets/` 下三个语言站共用的
静态资源，`build_tw.py` 只转 `tw/` 目录下的文件。于是繁体页上的挂件整块
变回简体：「问」「说说看……」「发送」「今天还能问 5 次」。

修法是让繁体表**由简体表生成**，不手写：手写的话改一处简体就得记得改
另一处，而记不住的事情迟早会漏。生成器是 `scripts/gen_chat_tw.py`，
已经进构建链（排在繁体站之前，因为它改的是共用资源）。

**闸：** `scripts/check_chat_tw.py` —— 重新生成一次，和文件里的对不上就拦。

## 21. 共用模板里写死 CJK 标点，只在另一种语言上发作

`seo/hw_theme.py` 的 `_dek()` 负责把条目页导语裁到「认人那半句」，靠找
「——」和「。」。英文的 summary 用单破折号「 — 」和半角句点「.」，两个
都找不到，于是**整块不裁**：英文条目页露出的是 `d[:140]` 那个硬切口，
「… a cook and a provincial governor. He was banished」后面直接没了，
而正文几行之下又把同一段完整印一遍。

**闸：** `scripts/check_dek.py` —— 判据两条：模板形制的导语必须在破折号后
的第一个句号处收住；认人那半句之后剩下的字不许在同页别处出现。
判据里**没有**「必须以句号结尾」——中文导语是手写的名词短语，本来就不带
句号，第一版拿收尾当判据，一口气把 159 条手写导语全判成缺陷。
闸门判错的时候，先怀疑判据抄的是哪一种语言的习惯。

## 22. 工具条被塞进了一个手机上会消失的容器

条目页和章节页的 `.mast-links`（页头那排导航）在 700px 以下是
`display:none` —— 手机上导航收起来，这是有意的。而语言切换和夜间模式那
一组工具是运行时注入的，注入代码挑宿主时挑中了它，于是**三种语言的条目
页、章节页、全集页，在手机上都没有语言切换、也没有夜间模式开关**：整组
尺寸 0×0。桌面端一切正常，所有闸门全绿。

同一个页头还有第二个毛病：工具条是绝对定位的、不占位，站名一长就从它底
下穿过去（繁体首页压 28px，英文「Human World Rules」压 64px，「Rules」半
个词看不见）。修这一条走了两个弯路，都值得记下来：

- `padding-right` 没用。站名是 flex 项、宽度按内容算，加内边距只是把盒子
  撑大，右边缘一点没退 —— 重叠反而从 64px 变成 87px（整个下拉框）。
- `max-width:calc(100% - N)` 也没用。那个 `100%` 是按父元素宽度算的，而父
  元素是 shrink-to-fit，宽度又取决于这个子元素 —— 循环，最后算出 27px，
  站名断成「Human / World / Rules」三行。得用视口单位。

根子上的解法是把标签缩短：「English」一个词就占 87px，改成「EN」（中文
改成「简」「繁」）之后，页头本来就放得下。

**闸：** `scripts/check_mobile.py` —— 从一条判据加到三条：不横向撑开、
语言切换和夜间模式必须真的**在**且有尺寸、页头两个矩形不许有交集。
第二条一上来就报出 8 个页面。

## 23. 「不要 CJK 字体名」拦错了对象

英文页的字体，前后判错过两次，两次都是判据抄错了层面。

第一次是**根本没有覆盖**：字体链接早就换了，可 `html[lang="en"]` 那段变量
覆盖从来没进过仓库（写在中文站样式表末尾，丢了两回）。当时补的闸量的是
「源码里有没有那段覆盖」—— 那是个代理，而代理会和它代表的东西脱钩。改成
渲染之后读 `getComputedStyle` 才算落在结果上。

第二次是**判据的内容错了**：我拦的是「英文页上出现 CJK 字体名」。可把主站
和原声站的英文页量到一起才看清，差别根本不在字体名，在**有没有下载**：

- 原声不下载任何网络字体 → 英文标题落到 Songti SC（macOS 自带），它的拉丁
  字形是 Times 一路的老式衬线，排英文本来就成立。
- 主站下载 Noto Serif SC → 英文标题落到思源宋体的西文字形，那套西文是为
  「和汉字并排」设计的（窄、低对比、重心跟着汉字身框走），单独排一整页
  英文就是「完全不适合阅读」。

同一个位置，同一类字体名，好坏差在下不下载。按字体名拦，会把系统里那个
**好用的**宋体一起拦掉 —— 而它正是要的那个。

**闸：** `scripts/check_en.py` 第 ⑬ 条 —— 两条判据：英文页不许下载 CJK
网络字体；解出来的字体栈必须**就是** `build_en.py` 里 `EN_DISPLAY` /
`EN_SANS` 那两个值（单一来源，有意改字体的人改一处，闸门跟着走）。

## 24. 又一次改了生成出来的文件

标签行在 375px 上把整页顶宽 17px（英文的「Situations」比中文的「境」长得
多，换成杂志标题字体又宽了一截）。父容器的 `display:flex` 写在行内 style
上，只能从样式表补 `flex-wrap` —— 我把那条 CSS 加进了 `index.html`，
跑了一遍构建，闸门照旧报红，而规则在三份产物里一份都没有。

`index.html` 是**生成的**：`scripts/force_chapter_ui.py` 每次构建都重写它。
今天这是第二次踩同一个坑，第一次是把英文字体覆盖写在 `assets/hw-entry.css`
末尾，写了两次、丢了两次。

判断一个文件能不能手改，看的不是它在不在仓库里、扩展名是什么，是
**有没有哪个脚本会写它**。`grep -rln "<文件名>" scripts/ seo/` 一句话的事，
比丢一次工作便宜。

**闸：** 无 —— 这一条靠的是闸门本来就该报红（`check_mobile.py` 确实报了），
问题出在我看见红之后改错了地方。真正管用的不是再加一道闸，是改之前先
grep 一下谁写这个文件。

## 25. 同一份排版，中文放得下、英文放不下

用户的原话：「界面上中文看起来正常，同样的英文排版就会显得别扭，需要进行
缩减才行」。这不是个别串翻长了，是一整类：

- **卡片栏宽**。`columns:150px` 给正文留 131px —— 放得下 8 个汉字，一句
  12 字的问句两行就完；同样 131px 只放得下**三个英文单词**，
  「It hit me and I can't cool down.」断成三行，每行三个词。
- **字体分工**。FONT 表按字体族整批替换，中文宋体用在哪儿、Playfair 就跟
  到哪儿 —— 11px 的标签、12px 的胶囊、16.5px 的卡片问句全成了高对比的
  Didone 展示体。宋体在小字号上**本来就是正文字体**，Playfair 不是。
  同一个位置，中文那边成立，英文那边不成立。
- **占位文字**。「搜你遇到的事：被裁了、睡不着、孩子不听…」直译成英文 62 个
  字符，输入框内容宽只有 285px —— 尾巴整段看不见，而被裁掉的正好是举例，
  也就是这句话唯一的作用。（量下来中文那句也超，317px；只是没人量过。）
- **站名**。「人类世界生存法则」8 个字 160px；「Human World Rules」在
  Playfair 20px 下 212px。按中文宽度定好的页头，英文放不下。

共同的形状：**版式参数是照汉字的信息密度定的**。汉字方、等宽、一个字顶
一个词，同样的像素宽度换成英文就少了一半信息量。照搬版式而只换文字，
必然在某个位置崩掉，而崩在哪儿取决于哪一处余量最小 —— 所以是「到处都
有点别扭」，不是「某一处坏了」。

**闸：** `scripts/check_mobile.py` 第 ⑤ 条 —— 文字必须放得进它的框
（scrollWidth 超出 clientWidth，或占位文字量出来比输入框内容宽还宽）。
这一条对三种语言一起判：中文那句超了很久没人发现，正是因为从来没有人
量过「放不放得下」这件事。

## 26. 版块类型是靠中文标题认出来的，英文页于是塌成一片

条目页的版式不是数据里标好的，是 `seo/hw_theme.py` 的 `_render_blocks()`
**按标题文字**认出来的：看到「分则」就渲染成 `section.point`（带 h2 和例子），
看到「金句」就收集起来最后单独成块，「对照」「延伸」「留下」各有各的版式。

英文站的标题是 "The parts · …" "Lines to keep" "Further" "What did this one
leave behind?" —— 一个都不认识。于是整页英文条目页塌成一串一模一样的
`section.sec`：中文那边有的引言框、分则卡片、金句块、对照块、延伸块，
英文一个都没有。用户的原话是「看起来与中文版相差甚远」。

而所有闸门全绿：结构上每一页都在、链接都通、没有中文残留 —— 没有任何一条
判据在问「这一页的版块类型对不对」。

同一个函数里还藏着两处「只对中文成立」的假设，英文一跑就露：

- 对照块按「名字：理由」切，用的是全角冒号。英文是「Name — why」。
- 延伸块按**空格**切名字，注释里还写着「条目名从不含空格」—— 那对中文成立，
  英文名几乎都含空格（Wang Yangming / C.S. Lewis / Winston Churchill），
  照切会切出 `/en/i/wang/` 和 `/en/i/yangming/` 这种从不存在的页面。

修法是两套标记都认（中文页上不会出现 "The parts"，英文页上不会出现
「分则」，所以不需要语言开关，也就没有「传错语言」这种失败模式）。

**闸：** `scripts/check_dek.py` —— 加第 ④ 条：中英同名条目的**版块构成**
必须一致（`section.point` / `aside.pull` / `section.quotes` / `div.contrast`
/ `div.ext` 各自有没有），差一类就拦。数量可以不同（内容多少不一样），
但「有没有这一类版块」不该因为语言而不同。

## 27. GEO 层是给模型读的，所以没人会顺手看一眼

上一篇新内容之后去核对同步，发现英文站的 `llms.txt` / `llms-full.txt` /
`feed.xml` 里**一个章节都没有** —— 只收了 30 个条目，而站上有 80 个章节，
章节才是真正讲道理的那一层。中文站 377 章全在里面。

原因很小：`build_en` 漏调了 `hw_chapters.write_indexes()`。

难的是为什么这么久没人发现。页面建出来了、sitemap 有、链接通、hreflang 对，
所有「页面在不在」的检查全绿 —— 人打开网站什么都正常。GEO 层是**给模型读
的**，它坏了不会有任何视觉症状：一个读 `llms-full.txt` 的模型看到的英文站
只有 30 页内容，而我们以为它看到 110 页。这一层没有读者会替你发现问题，
所以它比页面更需要闸门，不是更不需要。

补上之后又露出第二层：折进去的块是用**中文标签**写的
（`### 分则 ·`、`### 金句`、`Tags: 深度阅读`、摘要里的「——」和「。」），
英文的 llms-full.txt 读起来像一份中文文档。共用生成器里写死一种语言的标签，
和第 26 条是同一个形状。

顺带记一条流程：**新增一章不是加一段数据就完了。** 闸门逼出来的完整清单是
——写中文章节（3 分则 / 3 金句 / apply 要有局面·先问·用反了）、写英文章节、
在处境层加一个问题指向它、在今日一问里给它一条、跑 `scripts/gen_og.py` 生成
分享图、再跑构建链。少任何一步都有一道闸会红，这是好事：清单不用记在脑子里。

**闸：** `scripts/check_geo_sync.py` —— 每种语言的章节页数量，必须和它自己的
`llms.txt` / `llms-full.txt` / `feed.xml` 里出现的章节地址数量对得上。

## 28. 做「另一种语言」这件事，下次怎么少犯错

这一轮从「英文版有很多体验问题」开始，一路修到条目页版块、就地加重、GEO
层。回头看，绝大多数问题是**同一个形状**的，而我是一个一个撞上去才发现的。
写下来是为了下次不用再撞一遍。

### 那个形状

版式参数、阈值、分派条件，都是照**汉字的信息密度和形态**定的：

| 定的时候按的是 | 换成英文之后 | 症状 |
|---|---|---|
| 栏宽 150px ≈ 8 个汉字 | ≈ 3 个英文词 | 卡片问句断成三行 |
| 金句去重重合 10 字 | ≈ 2 个短单词 | 金句段整段消失 |
| 就地加重长度 18–145 字 | ≈ 3 个词起 | 挑不出，或挑出碎片 |
| 断句按「。！？」 | 一个都不匹配 | 整段算一句，超长被丢 |
| 打分靠「」、——、不是…而是 | 一个都没有 | 全 0 分，门槛 >0，全灭 |
| 版块靠「分则」「金句」认 | 认不出 | 整页塌成一串一样的 section |
| 条目名不含空格 | 英文名几乎都含 | 切出 /i/wang/ 这种不存在的页 |
| 占位文字 20 字 | 直译 62 字符 | 尾巴被裁，而尾巴正是举例 |
| 站名 8 字 160px | 212px | 页头放不下，被挡或断行 |
| 小字号用宋体 | 跟着换成展示体 | 12px 的 Didone，做作 |

一句话：**汉字方、等宽、一个字顶一个词。同样的像素和同样的阈值，换成英文
就少一半信息量。** 所以照搬版式只换文字，必然在余量最小的那一处崩掉 ——
表现是「到处都有点别扭」，而不是「某一处坏了」，这也是为什么一条条报 bug
很难报干净。

### 下次的做法

1. **先搜一遍常量**，不要等症状。凡是「按字数/像素/字符定的数」和「按中文
   标点或中文词判断的分支」，都是候选：
   `grep -n "[一-鿿]" 共用渲染器` + 找所有 `len(...) <`、`[:N]`、
   `startswith("中文")`、`in ("中文",...)`。这一轮所有问题都能被这两条命令
   捞出来，而我是逐个撞上去的。
2. **判据落在结果上，不落在代理上**。「源码里有没有那段覆盖」是代理，
   代理会和它代表的东西脱钩（脱钩了两次）；「渲染之后解出来的字体是什么」
   才是结果。同理：「页面在不在」是代理，「llms-full 里有几章」是结果。
3. **两套标记都认，不要加语言开关**。中文页上不会出现 "The parts"，英文页
   上不会出现「分则」—— 同时认不会打架，也就没有「传错语言」这种失败模式。
   加开关则多一个要传对的参数，和一类新的错。
4. **量之前先想清楚「正文」指哪一块**。量金句重复度时忘了剔除 script，
   FAQ 的 JSON-LD 里也印着金句，假报 70 条，差点据此把阈值调错。
5. **改之前先 grep 谁写这个文件**。`index.html`、`assets/hw-entry.css` 都是
   生成的，手改活不过一次构建 —— 这一轮踩了两次。
   `grep -rln "<文件名>" scripts/ seo/` 一句话的事。
6. **缓存版本号跟内容走**。改完 CSS 反复重建、页面纹丝不动，一度以为是选择
   器权重不够，其实浏览器在用 `?v=1` 的旧文件。
7. **选择器要压得过带 id 的那条**。页面自己的规则多半是 `#hwx .kc .t`
   （权重 1,2,0），`html[lang="en"] .kc .t` 是 (0,3,1)，id 那一位直接压死
   后面所有位。同权重时还要看源码顺序 —— 外链样式表排在内联 `<style>` 后面。

### 判据本身也会「只对一种语言成立」

`check_dek.py` 第一版拿「必须以句号结尾」当判据，一口气把 159 条**手写的
中文名词短语导语**全判成缺陷。闸门判错的时候，先怀疑判据抄的是哪一种语言
的习惯 —— 和被判的代码是同一个毛病。

**闸：** 无 —— 这一条是方法，不是判据。它对应的检查散在
`check_en.py` ⑬⑭、`check_mobile.py` ①–⑤、`check_dek.py` ①–④、
`check_geo_sync.py` 里，每一条都在上面的表里有一行。

## 29. 声明了不等于装上了

用户说主站英文字体不如原声舒服。我量了一遍，两边 `font-family` 都是
「Playfair Display, Source Serif 4」，就回说「已经是同一对了，差别只在字号」。

**量错了对象。** `getComputedStyle` 给的是**声明**，不是**装上的**。真相是
主站英文页 `document.fonts` 里只有一个面、还是 unloaded —— 一个 webfont 都
没加载，整站落到 Georgia 兜底。声明是对的，画出来的不是；用户看到的是画出来
的那个。

原因很小：`FONT` 表干的事是「把中文字体的 Google Fonts 地址换成英文的」，
而条目页和章节页**本来就没有那条地址**（中文站那两类页用系统宋体）——
换无可换。首页因为原来有那条地址，看着是对的，内页不对。

这是第 23 条的同一个毛病在**同一道闸**上犯第二次：那次是「源码里有没有那段
覆盖」这个代理和结果脱钩，这次是「声明的字体栈」和结果脱钩。判据每往结果
挪一步，就少一类看不见的失败。

**闸：** `scripts/check_en.py` ⑬ 第 ⓒ 条 —— `document.fonts` 里必须有一个
status 是 loaded 的同名字体。修法也从「替换」改成「主动挂上」：finish() 给
每个英文页补字体链接，不依赖「原来有没有一条可替换的地址」。

## 30. 一块没有读者的东西，坏了不会有人报

原声的分享卡在微信里是一块灰色占位图，用户截图我才知道。查下来 og:image 直接
指向各家播客的 CDN（bbci / megaphone / omnycontent…）：抓图的一方在墙内，这些
域名要么慢要么不通；就算通，给的也是 3000×3000、1MB 以上的原图，而 og 里声明
的是 600×600。

比这更糟的是：**318 个节目页根本没有 og:image**，分享出去一律空白。这个数字
没人报过，因为分享卡不在页面上 —— 你自己浏览网站一辈子也看不到它坏了。

和第 27 条（英文 GEO 层漏了全部章节）是同一个形状：**没有读者的那一层，
坏了不会有人替你发现。** 页面有人看，所以页面的毛病总会有人报；llms.txt、
og:image、feed 这些是给机器和转发场景看的，它们的判据只能靠自己写。

修法：`pipeline/cache_covers.py` 把封面缓存到本站（600×600 JPEG，192 张
10MB），`og_image()` 优先用本地的，没有封面或源站已 404 的退到站点默认图。
现在 1941 个页面：本集封面 1599、默认图 342、指向外站 0、没有图 0。

**闸：** 无 —— 这一条落在原声仓库，那边的判据是 `tests/test_render.py` 一族；
本仓库对应的同类判据是 `scripts/check_geo_sync.py`（第 27 条）。真正该记住的
是提问方式：**这一层的读者是谁？如果不是人，谁来告诉我它坏了？**

## 31. 拿字号当判据，等于拿汉字的排版当判据

用户发来手机截图：「信息流金句和问题卡标题还是旧的字体」。

同一屏里四张卡：人物卡的名字和新章卡的标题是 Playfair，金句卡的引文和问题卡的
问句是 Source Serif 4。更别扭的是问题卡内部 —— 上面 16.5px 的问句是正文字体，
下面 15px 的署名反倒是展示体，大的那行不如小的那行「像标题」。

根子在我自己写下的那条规矩：**「≥20px 的标题归 Playfair，正文归 Source
Serif 4」**。当时是为了拦住 Playfair 漏到 11px 的标签上，看着挺合理。它错在
判据本身：**那几个字号（16.5 / 19 / 21px）全是照汉字定的。** 汉字 16.5px
撑得住一行标题，拉丁字母撑不住。于是「字号 → 角色」这一步在英文里必然错判，
而且错得很隐蔽 —— 每张卡单独看都对，只有四张摆在一屏里才看得出不齐。

中文那边四种卡的标题是同一套宋体，一个声音；英文这边裂成两半。这是第 25 条
（参数是照汉字的信息密度定的）的又一次，但形状更刁：这次汉字的参数不是直接
用错了，而是**被拿去当判据用了**。判据必须落在角色上 —— 这一行是不是这张卡的
标题 —— 字号是角色的结果，不是它的依据。

顺手修掉同一张卡上的另一处：署名是「名字 + 问过」拼的，中文不用空格，英文要，
界面串表照搬过去就成了「Li Ka-shingasked」。

**闸：** `scripts/check_en.py` ⑮（`card_headline_voice`）——
在浏览器里量四种卡标题**渲染出来的**字体和字号，跨卡比对，不一致就拦；
同一条顺带查署名黏连。三个 tab 各铺一段不同的代码（ncCells / fullCells /
scCells），所以三个都要走一遍。反向注入两条：
`gate_selftest.py` 的「卡片标题不是一个声音」「卡片署名黏在一起」。

写这道闸的时候自己又踩了一次转义：JS 正则里的 `\b` 写在普通 `"""` 串里，
被 Python 先解释成退格符，浏览器拿到 `/…asked\x08/`，永远匹配不上 ——
闸门返回空数组，看起来一切正常。**凡是「Python 字符串里装另一门语言的源码」
一律用 `r"""`。** 这个形状已经咬过三次（`\201C` 当八进制、`\u` 当转义、
这次的 `\b`）。也正因为先跑了反向注入，才发现这道新闸是死的 ——
只验「好稿能过」的话，它会一直绿着。
