# 交接说明 · ourword.ai

写给下一个接手的 AI 代理。先读这一份，再读 `README.md`（工程约定）和
`DESIGN.md`（视觉规范，数值全部来自线上实测）。

线上：https://ourword.ai · 仓库：`woowoeth/woowoeth.github.io`

---

## 当前状态（截至本次交接）

```
条目 124 → 章节 276 → 处境 76 → 问题 229 → 答案 458
章节可达率 276/276 = 100%
```

**十个领域的条目分布**（数字暴露了内容洼地）：

| 领域 | 条目 | | 领域 | 条目 |
|---|---|---|---|---|
| 权力与组织 | 19 | | 心智与情绪 | 11 |
| 谋略与竞争 | 16 | | 世界如何运转 | 10 |
| 创业与产品 | 16 | | 学习与成长 | 10 |
| 财富与风险 | 15 | | 家庭与关系 | 9 |
| 识人与相处 | 12 | | **身心与生活** | **6** |

前四个领域（职场、商业、权力、投资）占了 66 个条目，
而家庭、身心两块加起来只有 15 个——这是已知的结构性偏斜，见下面「用户场景思考」。

---

## 这一轮做了什么

按时间顺序，每条都注明**为什么**——原因比结论重要，因为下一次判断要靠它。

### 一、定位从「装了什么」改成「能拿来干嘛」

标语从「115 个人物与典籍的生存智慧，跨越 2600 年」改成
**「遇到事了，看看以前的人怎么处理」**，五类页面统一。

前者描述内容，读者把它归类为「一个收藏」（被动）；后者描述用途，
读者才知道现在就能用。这是首次访问的三秒内唯一需要传达的信息。

### 二、首页顺序按定位重排

原来三块的顺序和权重与价值相反：今日一句 322px 在最上，今日一问 186px 排最后（665px 处）。
现在**今日一问置顶**（113px，用原来一句的体量），一句压缩，一篇缩小殿后。

今日一问还换了数据源：原来取 `ASK`（每篇 apply 里的「先问」句），
那是给**读完文章的人**自省用的、主语是读者、常常三连问，放到首页像考卷。
现在取 `QQ`（处境层问题），是人话，前面缀一句「某某也卡在同一件事上」——
读者只需**认出**，不必回答。

### 三、问题池全部重写并建立内容标准

**这是这一轮最重的一块。** 问题池是首页一问和信息流问题卡的共同来源，
问题本身不戳中人，卡片设计得再好都白搭。

新建 `scripts/check_questions.py`（已接 CI），四条否决项：

| 否决 | 反例 | 正例 |
|---|---|---|
| 查资料语气（怎么/如何开头） | 怎么让自己迈出第一步？ | 我知道该做，可就是动不了。 |
| 抽象名词堆砌 | 什么样的资产才是真正的资产？ | 钱放着不动，我心里发慌。 |
| 无人称无情境 | 砍掉哪些才是对的？ | 全都重要，我砍哪个都疼。 |
| 长度 8-18 字 | — | 短了撑不起情境，长了一眼读不完 |

建立时原有 114 条**58% 不合格**，全部重写。现在 229 条 100% 通过。

### 四、处境层：补缺口 → 分组 → 收成一行 + 独立 tab

三步，每一步都是上一步暴露出的问题：

**① 补 26 个处境**（50 → 76）。原来 50 个全是职场决策，家庭与身心是零，
而条目层早有 9 个相关内容——写了但用户找不到。补的三块：
人生转弯（突然没了工作、重要的人走了）、使不上劲（事情我说了不算、努力也没用）、
说不出口（看不得别人好、我做错了事、怕被看穿）。

**② 发现补了也白补。** 实测：76 个处境**用户默认只看得见 4 个**，
其余全折在「展开全部」后面——折叠逻辑按「两行」算。加多少门都是关着的。
于是把分组从注释提升为数据（每个处境加一个 `g` 字段，14 组），默认可见 4 → 28。

**③ 分组之后仍占 1049px，把信息流推到 2382px。** 而且发现一个更根本的问题：
处境层的 196 个问题**100% 已经在信息流里以卡片形式出现过**——
同一批内容首页呈现两遍，而目录那一遍还更差（给的是我们的分类标签、三步才到答案，
卡片给的是用户自己的话、一步到位）。

最终形态：**新增第三个 tab「处境」**（229 张问题卡，复用信息流已有的 `.kc` 样式），
原目录收成一行入口。信息流从 2382px 提到 1053px。

### 五、可达率 85% → 100%

42 篇章节此前没有任何问题指向它们，包括站里最好的一批：
孙子兵法《先胜而后求战》《知彼知己》、诸葛亮《隆中对》、甘地《食盐进军》。
33 条新问题全部接上，**没写一个字新内容**。

### 六、埋点（这一轮最后做的，也是最该早做的）

GA 装了很久但**零个自定义事件**——我们对用户行为一无所知。
这是这一轮两次返工的根源：加了 14 个没人看得见的处境、
建了一个和信息流重复的 1049px 目录，都是建完才发现。

五个事件已上线：

| 事件 | 回答 |
|---|---|
| `tab_switch` | 处境 tab 有人点吗 |
| `situation_entry_click` | 一行入口有人点吗 |
| `card_click` | 问题卡/人物卡/金句卡/章节卡哪种被点得多 |
| `search_hit` | 搜什么、搜到几条 |
| `search_miss` | **搜什么、我们没有** ← 最值钱 |

### 七、内容与其它

- 新增 15 个条目：阿德勒、非暴力沟通、塞涅卡、克里斯·沃斯、戈特曼、关键对话、
  费曼、罗杰斯、依恋理论、原子习惯、波普尔、艾利克森、蒙台梭利、瑞迪、纽波特，
  再加 9 个：契克森米哈赖、萨波尔斯基、BJ·福格、萨提亚、鲍恩、托马斯·戈登、
  维果茨基、德韦克、优秀的绵羊
- 类别 7 类改 10 领域制（「典籍·洞见」是形式不是领域，已拆散）
- 首屏性能：655KB 字体只被早已隐藏的组件引用，改按需加载，
  DOM 就绪 4652ms → 1827ms，传输 813KB → 155KB
- 删掉 116 个中文路径跳转桩及其生成器
- 章节页/条目页加分享收尾块
- `DESIGN.md`：九节视觉规范，16 项数值逐条与线上核对过

---

## 接下来的计划

### 立刻（等数据的这周可做）

**问题从 229 补到 ~345。** 现有 276 篇章节按「平均被引 ≤2.5 次」算，
上限约 345；现在平均 1.66，还有空间。**零新内容**，把 150 篇单引用的章节
做到 2 次即可。做了不会白做，不依赖任何数据。

### 一周后（有数据再定）

看三个数，再决定内容往哪补：

1. **处境 tab 点击占比** —— 低于 5% 说明这一层不该继续投入，
   甚至该考虑把它并回信息流
2. **四种卡片的点击分布** —— 问题卡如果明显高于人物卡，
   说明「认出」这条路是对的，值得把问题卡的比例再提
3. **`search_miss` 词表** —— 这个直接决定内容选题，比任何推理都准

### 中期

**内容洼地补齐**：身心与生活 6 个、家庭与关系 9 个，
而权力与组织有 19 个。但**别在没有数据前扩内容**——
每多 100 个问题需要约 80 篇新章节，成本要有证据支撑。

### 已废弃的计划（重要，别捡回来）

**「1000 个处境」这个 KPI 不成立。** 它是基于「一篇章节可服务 8 个处境」
算出来的，而**实测扇出只有 1.66**。按真实密度，1000 处境需要约 3000 篇章节、
1300 多个条目——是原估算的三倍多，以年计。

不要把 1000 当目标。用约束代替数字：每篇章节被引 1-4 次、平均 ≤2.5、
每个处境 2-5 个问题、可达率 100%。

---

## 用户场景思考（这部分是判断，不是事实）

### 定位那句话的三个词，各对应一块

**「遇到事了」——承接情绪的入口。** 试过三个方案（情绪四选一、句子填空、
现状平铺），产品负责人认为都不好，我同意：**它们都在替用户分类**，
要求他先把自己的处境套进我们预设的格子。真正该做的是反过来——
不问他是什么处境，直接给他看别人的处境，他只需**认出**。

**「以前的人」——人的在场感。** 现在条目页先看到的是「课题分离」这类概念名，
人退到后面。但标语承诺的是看**人**怎么处理的。故事应该在最前面，
概念从故事里长出来，而不是给概念做注脚。

**「怎么处理」——缺「他当时也不确定」。** 现在所有内容读起来都像
已经有答案的人在讲道理，而用户此刻没答案。**失败和犹豫比结论更有说服力**——
粟裕第一次上书被驳回、费曼也会骗自己。现有 23 条写了「败局时刻」，
这个字段应该被放大而不是当补充。这一块最缺也最便宜，是写作重心的调整，
不需要新功能。

### 用户画像偏斜

按角色数处境：管理者 11、创业者 9、投资者 5——**25 个处境假设你在管人或管钱**。
缺的是：学生与刚工作的人（第一份工作、要不要读研）、自由职业（接不到活、
客户跑了）、不在职场的人（全职照顾家庭、待业、退休）、女性特有处境
（产假回归、生育决定）。

这不是政治正确问题，是市场问题——中文互联网上找「生存智慧」的人，
远不止创业者和管理者。补它需要新内容，不是重新映射能解决的。

### 两块内容撑不住、故意没开的处境

**愧疚**（全站只有 3 篇相关章节）和**自我怀疑**（5 篇）。
开了处境却只能给两个勉强相关的答案，**比不开更伤**——
用户点进来发现答非所问，下次就不点了。等有对应内容（欧文·亚隆、
罗杰斯那一批）再开。

### 关于信任（产品负责人问过，值得写下来）

不该整体判断内容可靠性，要分层：

- **工程与结构**：有客观判据、机器可验，可靠
- **中国古代典籍引语**：原文流传稳定，相对可靠
- **当代英文著作的 `==引语==`**：**是中译而非出版原句**。
  戈特曼、卢森堡、沃斯、《关键对话》的引语全部如此。
  数字（如戈特曼九成准确率）在学术上有争议
- **德韦克**收录时打过折扣：整整一篇讲明效应量远小于流行说法、
  在部分人群接近于零——这是我们和鸡汤号的区别

三条不依赖信任的验证法：随机抽一句搜、要求先自曝存疑清单、把标注做进产品。
引语分级标注（原文/译述/据报道）产品负责人明确说**先不加**。

---

## 工作纪律（务必遵守）

### 每次提交前必跑

```bash
python3 scripts/check_chapters.py
python3 scripts/check_questions.py      # 问题内容标准
python3 scripts/check_coverage.py       # 处境层覆盖
python3 scripts/check_integrity.py      # 完整性
python3 scripts/check_repetition.py
python3 scripts/check_quote_overlap.py . 10
python3 scripts/patch_geo_seo.py && python3 scripts/apply_redesign.py \
  && python3 seo/build_seo.py && python3 scripts/force_chapter_ui.py
```

最后一行**连跑三遍，产物必须字节相同**（幂等闸门）。

### 加条目时四样必须同时补齐

1. `index.html` 的 `D[]` —— 条目本体（`l`/`contrast` **只能指向已存在的条目**）
2. `seo/chapters/<slug>.py` —— 至少一篇章节
3. `scripts/hwx_scenes.py` —— **给新章节挂上问题**（最容易漏，`check_coverage` 会拦）
4. `scripts/force_chapter_ui.py` 的 `HWX_INTROS` —— 首页手写介绍

外加手动跑 `scripts/gen_og.py`（**不在 CI 链里**）。

### 推送

`python3 /home/claude/ghkit.py push /home/claude/repo woowoeth/woowoeth.github.io`
**每 UTC 日 40 次上限**，日切自动重置。

### rebase 冲突

几乎总在 `index.html`（CI 每次构建后自提交产物）。它既是产物又是数据源，
逐行 merge 无意义。做法：**取远端作底 → 重新注入本批新条目 → 重跑构建**。
直接 `checkout --ours/--theirs` 会静默丢条目（已发生三次）。
rebase 语境下 `--ours` 指远端。

### 三条硬规矩

1. 不要直接改 `geo_kit.py`——CI 会从固定 commit 拉回覆盖，改动写进 `patch_geo_seo.py`
2. 改了 CSS 内容一定要升 `?v=`
3. 改源头不要改产物——`index.html` 是构建产物也是数据源，
   改数据改 `D[]`，改样式改 `scripts/force_chapter_ui.py`

---

## 给接手者的两句话

**第一，别在没有数据的时候扩产。** 这一轮两次返工都是同一个模式：
建完才发现没人看得见、或者和已有的东西重复。埋点刚上，等一周比猜一个月值。

**第二，问题的品质比任何界面都重要。** 处境层、问题卡、今日一问
共用同一个问题池——一句话戳不中人，三个界面全部失效。
改问题池之前先读 `scripts/check_questions.py` 的文件头。
