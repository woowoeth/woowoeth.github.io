# DESIGN.md · 人类世界生存法则

给 AI 编码代理和接手的人读的设计系统。**所有数值来自线上实测**，不是设计稿上的意图，
是浏览器里量出来的计算值。改了样式请回来更新这份文件。

站点：https://ourword.ai · 定位：遇到事了，看看以前的人怎么处理

---

## 1. 视觉基调

**宣纸与朱印。** 这套视觉不是从任何设计系统抄的，是从内容里长出来的——
两千年的典籍与人物，配一个像纸、像印章、像旧书的界面。

三个决定基调的选择：

- **暖米底而非纯白**（`#f5f1e8`）。纯白像应用界面，米底像纸。
- **朱红作唯一强调色**（`#a33b2e`）。取自印章的朱砂，全站只有它一个高饱和色，
  用在人名、标红、印章、组标题上；其余全是墨与灰。
- **宋体承担识别，黑体承担阅读**。标题、卡片名、金句用 Noto Serif SC；
  正文、元信息、标签用系统黑体。这条对比撑起了整个层级——不靠加粗和放大。

**密度收过一次（2026-08-31）。** 原先移动端一屏 5.8 张卡，产品负责人认为
信息量太大、撑不住阅读专注度。

试过改单列（列宽 330px）：一屏 4.2 张，卡片因为变宽反而从 263px 矮到 178px。
数字最好看，但**单排的样子被否了**——双列的错落感是要保留的。

最后的做法是**双列不动，给卡片加一行真内容**：人物/章节卡补上
「什么时候翻开它：<局面句>」。人物卡 263px → 347px，一屏 5.8 → 4.7 张。

- ✅ **降密度要靠加有用的内容，不靠加留白。** 812px 视口两列要一屏 4 张，
  每张得 406px；靠空白撑到那个高度，卡片里会出现大片留白，像没写完。
- ❌ **不要为了凑数字灌内容。** 4.7 是加真材料能到的位置。再往下压需要每张
  再添两行不存在的东西——那是灌水，不是设计。

密度仍然是价值主张的一部分，但它由**总量**表达（125 位人物、278 篇深度阅读、
317 个问题），不由**每屏塞几张**表达。章节页依旧是一屏只有一件事。

**几乎没有阴影。** 全站 `box-shadow` 用了 4 处，其中两处是描边式（`0 0 0 1px`，
等价于细线）：`#q` 搜索框、`#panel` 浮层；两处是真正的浮起：`#dqCard` 分享卡
（`0 8px 32px rgba(0,0,0,.45)`）、`#hwx-theme` 主题按钮（`0 1px 6px`）。层次仍靠底色和 1px 细线区分，
不靠浮起。这是纸的逻辑，不是卡片的逻辑。

---

## 2. 色板与语义

### 浅色（默认）

| 变量 | 值 | 用途 |
|---|---|---|
| `--paper` | `#f5f1e8` | 页面底色 |
| `--paper2` | `#eee8da` | 次级面（金句卡、收尾块） |
| `--card` | `#faf7f0` | 卡片底，比页面略亮 |
| `--ink` | `#1f1c17` | 正文与标题 |
| `--muted` | `#8a8377` | 元信息、说明、组标题 |
| `--line` | `#d8d2c6` | 分隔线、卡片边框 |
| `--acc` | `#a33b2e` | **唯一强调色**：人名、标红、印章、朱字 |

### 深色

| 变量 | 值 | 说明 |
|---|---|---|
| `--paper` | `#171410` | 不是纯黑，仍带暖调 |
| `--paper2` | `#201c15` | |
| `--card` | `#1d1913` | |
| `--ink` | `#eae3d4` | 不是纯白，降低刺目 |
| `--muted` | `#9a917f` | |
| `--line` | `#3a342a` | |
| `--acc` | `#c65f4f` | 朱红提亮，暗底下 `#a33b2e` 对比度不足 |

**深色不是把浅色反相。** 条目页与章节页有独立的一整套变量
（`--bg` / `--surface` / `--ink-2` / `--seal` / `--rule`），
曾经因为只改了首页那套，章节页正文对比度只有 1.63、深度阅读块 1.26——
改深色时三类页面都要实测。

### 半透明用法

| 场景 | 值 |
|---|---|
| 问题卡底 | `rgba(163,59,46,.07)` |
| 问题卡边 | `rgba(163,59,46,.22)` |

朱红淡洗，让问题卡在瀑布流里可辨，又不至于跳出来。

---

## 3. 字体层级

字族只有两个：

```
衬线（识别）  "Noto Serif SC","Source Han Serif SC","Songti SC","STSong",serif
无衬线（阅读） -apple-system, "PingFang SC", "Hiragino Sans GB", sans-serif
```

### 首页

| 元素 | 字号 / 行高 / 字重 | 字族 |
|---|---|---|
| 字标「生存法则」 | 20px / — / 700 | 衬线 |
| 标语 | 12px / 1.4 / 400 | 无衬线 |
| 区块标题（按处境找） | 21px / 1.55 / 700 | 衬线 |
| 今日一问 · 问题 | 21px / 1.75 / 700 | 衬线 |
| 今日一问 · 谁在问 | 13px / 1.7 / 400 | 衬线，朱红 |
| 今日一问 · 局面 | 13.5px / 1.8 / 400 | 无衬线，muted |
| 今日一句 | 17px / 1.8 / 700 | 衬线 |
| 人物卡 / 章节卡名 | 19px / 1.45 / 700 | 衬线 |
| 卡片引语 | 15.5px / 1.85 / 400 | 无衬线 |
| 问题卡标题 | 16.5px / 1.65 / 700 | 衬线 |
| 竖排金句 | 17px / 1.85 / 700 | 衬线（**必须显式指定 CJK 衬线**，见第 7 节） |
| 处境标签 | 14.5px / 1.6 / 400 | 无衬线 |
| 处境组名 | 11.5px / 1.4 / 400 | 衬线，muted |
| tab | 17px / — / 600 | 衬线 |
| 搜索框 | 16px | 无衬线（**不可低于 16px**，否则 iOS 聚焦时自动缩放） |

### 章节页（阅读态）

> 以下为 **375px 实测**。桌面（1280px）对应值是标题 40px、一行 32 字、
> 首屏 13 个可点元素——差别来自宽度，不是另一套规范。

| 元素 | 值 |
|---|---|
| 标题 | 28px 衬线 |
| 正文 | 17px / 1.75 / 无衬线 |
| 一行字数 | 20 字 |
| 标红 | `#9d2933` / 700 / **无下划线** |
| 首屏可点元素 | 5 个 |

**首页与章节页的差别在可点元素数量，不在排版。** 首页一屏并列 6 张卡（浏览态），
章节页首屏只有 5 个可点、其余全是正文（阅读态）。

---

## 4. 组件

### 卡片（信息流）

```
圆角 14px · 内距 16px 16px 14px · 边框 1px var(--line) · 底 var(--card)
下边距 12px · 无阴影
```

三种卡片靠**印章**区分，不靠底色——双列瀑布流里底色差异看不出来：

| 类型 | 标识 | 底色 |
|---|---|---|
| 人物 / 章节卡 | 无印 | `--card` |
| 金句卡 | 右上「句」印 + 竖排 | `--paper2` |
| 问题卡 | 右上「问」印 + 「谁问过」 | 朱红 7% |

**印章规格**：22×22px，1.5px 朱红边框，圆角 4px，衬线字，定位 `top:13px right:13px`。

### 处境选择条（处境 tab 顶部）

```
两行横滑 · grid-auto-flow:column · grid-template-rows:auto auto
列宽 max-content · 行距 7px 列距 6px · 高 79px · overflow-x:auto
标签：圆角 999px · 内距 7px 14px · 1px var(--line) · 14.5px · line-height 1.3（实测高 35px）
选中：墨底反白（--ink / --paper）
```

77 个标签（76 个处境 + 一个「全部」），总宽约 4371px，靠横滑看完。
竖着平铺是 1049px——那正是被废掉的旧目录。

- ✅ **按组排序，但不显示组名。** 组名与标签同行会混成半个标签（见下方「组件」一节），
  改用排序让同组的处境自然相邻，滑到「跟伴侣吵」旁边就是「孩子不听」。
- ✅ **切回该 tab 时把选中的标签滑进视野**（`scrollIntoView({inline:'center'})`）。
  选择跨 tab 保留，若条子停在开头而卡片是筛过的，人会以为坏了。
- ✅ **右上角计数只报问题数**，不报「N 种处境」——横滑条已经把处境摆出来了，
  再报一遍会在 375px 上折行撞进 tab 下划线。

### 按钮

| 类型 | 样式 |
|---|---|
| 主按钮（保存卡片、分享） | 墨底反白，圆角 999px，内距 5-7px / 12-16px |
| 次按钮（换一换） | 透明底，1px 边框，同圆角 |
| 处境标签 | 圆角 999px，内距 7px 14px，边 `--line`；选中态墨底反白 |

**主按钮必须是墨底反白。** 曾经因为弱化整块把它改成 `var(--muted)`，
结果最该突出的按钮成了三个里最浅的。

### 标红

```html
<mark class="hl" style="background:transparent;color:#9d2933;font-weight:700;text-decoration:none">
```

颜色加粗已经足够标记重点，**不要加下划线**——一页有多处标红时会很吵。

---

## 5. 布局与间距

### 栅格

| 项 | 值 |
|---|---|
| 页面左右内距 | 24px（**所有宽度恒定**，是刻意选择，见 §7）|
| 信息流列宽 | 150px（`columns`，非 grid）|
| 列间距 | 12px |
| 卡片下边距 | 16px |
| 宽屏列宽 | 230px（`@media(min-width:900px)`）|

**用 CSS 多列，不用 grid。** grid 会强制同行等高，瀑布流的错落就没了。
代价是多列按顺序灌满一列再下一列，最新的会集中在左列——这是已知取舍。

### 断点实测

| 宽度 | 列数 |
|---|---|
| 360 / 375 / 390px | 2 |
| 620px | 3 |
| 760px | 4 |
| 900px+ | 3-4（列宽转 230px）|

375px 实测卡片中位高：人物卡 347px、金句卡 330px、条目卡 289px、问题卡 234px，
一屏约 4.7 张。问题卡和条目卡矮是因为它们各自已有「什么时候用」的表达
（处境行、落地行），不需要再补。

### 垂直节奏

| 位置 | 值 |
|---|---|
| 页头 → 最近看过 | 12px |
| 最近看过 → 首个卡片 | 14px |
| 区块之间 | 14px |
| 处境组标题上方 | 9px |

**媒体查询必须放样式表末尾**，否则被基础规则覆盖——这个坑踩过两次。

⚠️ **但页头和 `.wrap` 左右内距不受这条支配。** `assets/hw-home-lockup.css`
在 `<style>` 之前加载，却每一条都带 `!important`——`.wrap` 的左右内距被钉死在
`max(24px, env(safe-area-inset-left))`，`.brand-logo` 钉死 36px，`.hd-title` 钉死 20px。
无论写在哪、写多少遍，`index.html` 里的对应声明都不生效。**改页头改 `hw-home-lockup.css`。**
（`index.html` 里那批死声明已于 2026-08-31 清掉，只留下 lockup 没接管的 `white-space`。）

---

## 6. 层次与深度

**不用阴影建立层次**（4 处 `box-shadow` 里只有 2 处是真浮起，见第 1 节）。三种手段替代：

1. **底色差**：页面 `--paper` → 卡片 `--card`（略亮）→ 次级面 `--paper2`（略暗）
2. **1px 细线**：`--line` 的边框和分隔线
3. **虚线**：卡片内部的分隔用 `1px dashed`，比实线轻

层次顺序（从后到前）：页面底 → 次级面 → 卡片 → 印章/强调色。

---

## 7. 该做与不该做

每一条都对应一次真实事故。

### 字体

- ✅ 竖排金句**必须显式指定 CJK 衬线字体**。系统 UI 字体没有竖排度量，
  字符推进量会算成 1px，整块糊掉。
- ✅ 竖排容器用 `width:fit-content` + 固定高度，不用 `flex:1`。
- ❌ 不要为装饰性功能预加载大字体。曾经一个 655KB 的字体只被早已隐藏的旧组件引用，
  首屏从 0.6 秒拖到 4.65 秒。**功能隐藏了，加载也要跟着停。**

### CSS

- ✅ 改了 CSS 内容**一定要升 `?v=`**，否则浏览器用旧缓存。
- ✅ 覆盖规则和媒体查询放样式表末尾。
- ❌ 不要假设 `.flow > *` 能压住 `.card{margin:0}`——**权重相同时后定义的赢**。
  用 `.flow > .card` 提权。
- ❌ 不要用 `:has()` 做关键覆盖，注入的样式常常排在目标规则之前。

### 组件

- ❌ **组标题不要与标签同行**。试过为省高度这么做，结果组名混在标签流里像半个标签，
  比不分组更乱。高度靠"每组只露 2 个"来收。
- ❌ **装饰性符号不要占一整行**。问题卡原来用一个大「？」占一行却不传递信息，
  换成「谁问过」+ 小印章。
- ❌ 分享按钮不要做大。两个 pill 按钮占的空间和一节正文相当，会把刚读完的注意力拽走。

### 留白宽松是选定的，不是没改

- ✅ **小屏左右留白保持 24px。** `index.html` 里曾有一条
  `@media(max-width:480px){.wrap{padding-left:16px;padding-right:16px}}`，
  被 `hw-home-lockup.css` 的 `!important` 压着，**从未生效**，2026-08-31 已删。

  删之前在 375px 上实拍过两版对比：16px 版正文宽度从 327px 涨到 343px（+5%），
  标题 20px→22px，今日一问的局面句从两行收成一行、金句从三行收成两行——
  更紧凑，一屏能多塞小半张卡。**产品负责人看过对比后选择保留 24px 的宽松版。**

- ❌ **不要因为「代码里写过 16px」就把它恢复。** 它不是待修的 bug，是一个已经
  做过的选择。要改留白得当成一次正经改版：重定 360/390/414 各档、
  测标题与右上角主题按钮会不会撞（22px 时明显更近），再回来改这份文档。

### 内容与结构

- ✅ **条目名不能含空格**。「延伸」区按词切分渲染，`BJ 福格` 会变成
  `/i/bj/` 和 `/i/福格/` 两个死链。用 `·` 代替。
- ✅ 首页数字（条目数、标语）**从数据生成**，不要手写。手写过三次，每次加内容都漏改。
- ❌ 不要用正则删带嵌套的 DOM 块。正则会连块自己的 `</div>` 一起吃掉，
  替换串再补一个就会提前关闭父容器，整页边距消失——而所有既有检查都会通过。

### 深色模式

- ✅ 首页、条目页、章节页各有独立的变量表，**三处都要改、三处都要实测对比度**。

---

## 8. 响应式

| 断点 | 变化 |
|---|---|
| `<480px` | logo 36px（页面内距**仍是 24px**，见 §7「留白宽松是选定的」）|
| `<600px` | `.wrap` 下内距 8px；字号由 lockup 恒定，不随断点变 |
| `<620px` | 处境栅格间距 8px |
| `<900px` | 信息流列宽 150px（2 列）|
| `≥900px` | 信息流列宽 230px |

**触控目标**：主按钮和标签 ≥32px 高。信息流卡片整块可点，不依赖小热区。

**搜索框字号不得低于 16px**——iOS Safari 会在聚焦时自动放大页面。

---

## 9. 给 AI 代理的提示

### 快速色值

```
底 #f5f1e8   卡 #faf7f0   次面 #eee8da
墨 #1f1c17   灰 #8a8377   线 #d8d2c6
朱 #a33b2e   标红 #9d2933
暗底 #171410  暗墨 #eae3d4  暗朱 #c65f4f
```

### 现成提示词

> 用宣纸底色 `#f5f1e8`、墨色 `#1f1c17`、朱红 `#a33b2e` 三色。
> 标题和卡片名用 Noto Serif SC，正文用系统黑体。
> 卡片圆角 14px、1px 细边框、无阴影。
> 强调色只用朱红一处，其余全是墨与灰。

### 改动前必读

1. 数值以**线上实测**为准，不以这份文档为准——文档可能滞后，改完请回来更新
2. 改样式的入口在 `scripts/force_chapter_ui.py`（首页）和
   `assets/hw-entry.css`（条目/章节页），**不要直接改 `index.html`**，它是构建产物
3. 提交前跑幂等三连：构建脚本连跑三遍，产物必须字节相同

---

*最后更新：随代码走。数值有出入时以浏览器实测为准。*
