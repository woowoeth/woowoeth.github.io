# 加一个人 / 一本典籍：要动哪些地方

这份清单是**操作性**的：每一行都是「不做会怎样」，以及「哪道闸会拦住你」。
英文那半的写作规矩在 `seo/en_batches/HOWTO.md`，这里只讲**结构**。

---

## 零、先分清哪些文件能手改

判断一个文件能不能改，看的不是它在不在仓库里，是**有没有哪个脚本会写它**：

```bash
grep -rln "<文件名>" scripts/ seo/
```

一句话的事，比丢一次工作便宜（FAILURES 第 24 条：同一个坑踩过两次）。

**生成物，永远别手改：**
`i/` `en/` `tw/` `all/` `t/` `feed.xml` `sitemap.xml` `llms.txt` `llms-full.txt`
`assets/hw-chat-index*.json` `seo/lastmod.json` `assets/hw-chat.js` 里的 `tw:` 那张表

`index.html` 是**半生成**的：多个脚本会打补丁进去，但 `const D=[…]` 那段
条目数据是手写内容，补丁不会动它。改样式或结构一律去改生成它的脚本。

---

## 一、中文（唯一的内容源头）

| # | 改哪里 | 写什么 | 漏了会怎样 |
|---|---|---|---|
| 1 | `index.html` 的 `const D=[…]` | 条目记录：`c n e w y d story f[] apply q l contrast`，可选 `fail lesson` | 首页信息流里没有这个人 |
| 2 | `seo/chapters/<slug>.py` | `PARENT` + `CHAPTERS`（`k n w src dek story f[] q apply`） | 没有章节页 |
| 3 | `seo/hw_slugs.py` | 中文名 → slug | slug 会被自动音译，多半不是你要的 |
| 4 | `seo/hw_kind.py` | 这条是「人」还是「作品」 | schema.org 把《稀缺》标成 Person |
| 5 | `scripts/hwx_scenes.py` | 每一章至少一个处境问句 | **闸「处境覆盖」拦** |
| 6 | `scripts/quote_asks.py` | 每一章一句今日一句（必须与某条处境问句逐字相同） | 闸「英文站/处境覆盖」拦 |
| 7 | `scripts/force_chapter_ui.py` 的 `HWX_INTROS` | 一句话介绍：卡片上是读者第一次碰到这个名字的地方 | 卡片上只有名字，读者不知道为什么该读他 |
| 8 | 同上的 `SC_BOX` | **只有新开处境时**才要：点开处境后输入框里的预填句 | 构建时硬断言直接抛异常 |
| 9 | 同上的分类配色 | **只有新开分类时**才要 | 芯片没有颜色 |

新开处境的预填句**不能是问句的复述** —— 两者在屏幕上相距三厘米，
构建时按四字片段查重。

## 二、繁体（全自动，但有一处要人）

`scripts/build_tw.py` 从简体页转出整站，条目、章节、处境、今日一句都跟着走。
**唯一需要人的是歧义字**：一简多繁里两边都讲得通的那些字，
每处的上下文要登记进 `scripts/tw_allow.txt`：

```bash
python3 scripts/gen_tw_allow.py     # 看一眼对不对，对就登记
```

漏了 → 闸「繁体站」拦（「歧义字上下文没登记过 N 处」）。

## 三、英文（独立的一条构建链，不是翻译）

| # | 改哪里 | 写什么 |
|---|---|---|
| 10 | `seo/en_names.py` | **先定名**：英文名 + 英文分类。名字是接口不是内容 —— 条目之间靠名字互相引用，各起各的名字会让交叉链接当场断掉 |
| 11 | `seo/chapters_en/<slug>.py` | 章节。**`k` 必须和中文逐字相同** |
| 12 | `seo/en_batches/bNN.py` | 条目记录 + `INTROS` + `SCENES` + `ASKS`（+ 新处境的 `SC_BOX`/`SC_SHORT`） |

写法、长度限、十条会被拦下来的事：见 `seo/en_batches/HOWTO.md`。
自检（几秒，不起服务）：

```bash
python3 scripts/check_batch.py bNN
```

**英文不是翻译。** 同一个事实、同一个论证，用英文读者自己的语感重写；
原话本来是英文的一律还原原句，不从中文回译；引号里的字一个都不许改；
没把握出处的引语一律不用。

---

## 四、跑完这四条再提交，**次序不能换**

```bash
python3 scripts/build_all.py      # 第一遍：把新条目的页面目录建出来
python3 scripts/gen_og.py         # 分享图，必须在**最后一遍** build_all 之前
# 英文分享图不用单独跑：build_en 会在清空并重建 en/ 之后自己出图（scripts/gen_og_en.py），check_en ⑰ 守着
python3 scripts/build_all.py      # 第二遍：build_tw 这次才复制得到 og.png
python3 scripts/gate.py           # 十八道闸
python3 scripts/gate_selftest.py  # 反向验：注入缺陷必须被拦下
```

**加新人的时候 `build_all.py` 要跑两遍，`gen_og.py` 夹在中间。**
原来这里写的是「`gen_og.py` 必须排在 `build_all.py` 前面」，对老条目成立，
对**新条目一定不成立**：`gen_og.py` 只给已经存在的页面目录出图，而
`i/<新 slug>/<章>/` 是第一遍构建才建出来的。所以照旧次序跑，第一句话
打印的是

```
跳过（页面目录不存在）: …/i/<slug>/<章>
```

三张图一张都没出，而它 exit 0，看着像跑成功了。
（2026-09-07 波斯曼这一条踩到：`gen_og.py` 排在前面，三张图全跳过。
日更每天都是新 slug，所以这不是特例，是每天的正常次序。）

第二遍**不必跑整条链**：og.png 只影响 `build_tw`（它从简体站复制二进制），
所以第二遍跑 `scripts/build_tw.py` + `scripts/stamp_assets.py` 就够。
整条链一遍约 15 分钟，两遍半小时；这样能省掉十几分钟。
（`stamp_assets.py` 不能省 —— 它扫的是最终产物，繁体站重转之后要重新盖章。）

**为什么最后一遍 `build_all.py` 之前必须有图。** 分享图不在构建链里，
所以直觉上它「什么时候补都行」—— 不行。`build_tw.py` 是**从简体站复制**
二进制文件来铺繁体站的，跑到它的时候 `i/<slug>/<章>/og.png` 还不存在，
它就什么都不复制；简体站和英文站都正常，只有繁体站那几页的 og.png 缺，
「语言站链接」那道闸报的是：

```
x /tw/i/<slug>/<章>/ → /tw/i/<slug>/<章>/og.png 这个页面不存在
```

看起来像分享图没生成（其实生成了），实际是生成得太晚。
补救办法是把 `gen_og.py` 跑一遍再**重跑一次** `build_all.py`。

（2026-09-06 薇依那次踩到：照旧文档的次序跑，十八道闸红了三道。）

---

## 五、最容易漏的五件事

**① 中文写了、英文没写。** 这个洞存在了很久：站里所有中英比对的判据都写成
「两边都有这一页时，比它们的内容」，中文有而英文没有的从那个 `continue`
静默溜走。英文站一度只有 40 条而中文 159 条，十六道闸全绿 ——
那 119 条不是写坏了，是从来没有任何一条判据**要求过它们存在**。
现在有闸了（`check_parity.py`），但记住这个形状：
**判据落在「有没有」上，才拦得住「少了一整块」。**

**② 照着中文站的路径写死。** `patch_chapter_scene()` 里一句
`os.path.join("i", …)`，结果「同一处境，还有人这么问」这一整块中文
376/377 页有、繁体 376/377 有、**英文 0/377** —— 而它正是这个站做共情的地方。
写任何页面级的加工，先问一句：这个函数跑不跑 `en/`？

**③ 拿汉字的参数去判英文。** 字号、阈值、行数、字距全是照汉字的信息密度
定的。汉字一个字就是一个信息单位，英文一个词才是 —— 同样的框，英文装不下
同样多的话。症状是「哪儿都别扭」，不是「某一处坏了」。

**⑤ 改了 Worker 不算数，Cloudflare 上重贴了才算数。** 问答的 Worker
（`worker/chat.js`）不在构建链里、不在 CI 里，是人去 Cloudflare 后台粘贴部署的。
2026-09-04 写好的英文提示词在仓库里躺到 09-07 才发现没生效 —— 三天里英文站的
读者问英文、拿到的全是中文答案。页面正常、请求 200、十八道闸全绿。
改了那个文件就跑 `python3 scripts/check_chat_lang.py`（比对部署指纹，不花 API），
或 `--live`（真问一句英文，看答案的语言）。文件头的 `BUILD` 常量要跟着改日期。

**④ 改了文件不算数，URL 变了才算数。** 样式和脚本带版本号引用，
`scripts/stamp_assets.py` 在构建链末尾按内容哈希统一盖章。别手写版本号 ——
手写的那种，新访客一切正常，只有老用户吃缓存，而你自己几乎总是新访客。

---

## 六、节奏：每天一个，不再批量

2026-09-06 起改成**每天一个人或一本典籍**（挂在 Claude 的每日任务上，
每天上午跑）。批量那次是补历史欠账（英文从 40 条补到 159），一次性的。

改回每天一个，是因为批量会把「值不值得」这道判断挤掉：二十个写手并行时，
指令是「把这 119 个人写完」，判据变成完整度；而这个站的判据应该是
**这个人能罩住多少种真实处境**。一天一个，才有空间问「今天这一个，
补的是哪个处境的哪一句」。

选法：先数出每个处境底下挂着几篇，挑最空的，再看清楚那里现在挂的是谁 ——
选一个**说出了现有那几篇没说出的那句话**的人。判据是「他补的是哪一句」，
不是「他也讲这个题目」。

```bash
python3 - <<'EOF'
import sys,collections
sys.path.insert(0,'scripts')
from hwx_scenes import SCENES
cnt={t:sum(len(r) for _q,r in qs) for t,g,qs in SCENES}
grp={t:g for t,g,qs in SCENES}
for t,n in sorted(cnt.items(), key=lambda x:x[1])[:12]:
    print("%2d 篇  【%s】%s" % (n, grp[t], t))
EOF
```

## 七、日更之后，首页那几个「今日/最新」也得跟着改

改成每天一条不只是内容节奏变了。首页有三处入口写着「今日」「最新」，
而它们原来的实现和「今天加了谁」**毫无关系**，日更等于白更：

| 入口 | 原来 | 现在 |
|---|---|---|
| 最新 | 按章节文件的 git **首次提交时间**排 —— 而构建跑在提交之前，新文件还没进 git，时间戳算 0，排到最后：**结构上永远看不到今天加的** | 未进 git 的文件按 now 算 |
| 今日一篇 |  纯轮播 | 取  最后一条（新条目追加在末尾） |
| 今日一句 |  纯轮播 | 开局落在最新那个人的金句上，点「换一换」再进轮播 |

三条都由  守着。共同点：不报错、不溢出、
别的闸全绿，**只是每天新写的东西在首页上没有位置**。
以后再加类似的「今日/推荐/精选」入口，先问一句：它看不看得见今天加的那一条。

## 八、一个人的工作量参考

现代人物 2 章、古籍 3–5 章。一个人 = 中文 2 处文件 + 5 处表 + 英文 3 处文件。
119 个人 / 272 章那次是二十个人并行写的，靠的是三件事：
每批只写自己的文件（`seo/en_batches/`）、名字先定后写（`seo/en_names.py`）、
秒级自检不抢端口（`scripts/check_batch.py`）。

## 九、这个仓有并行会话：日更多花的两小时全在这上面

2026-09-11 赫希曼那天，写内容 40 分钟，剩下的时间都花在下面两件事上。
两件都和内容无关，两件都会重演。

**① 开工前和构建前各看一眼 `git log -1`。** 那天写到一半，另一个会话
把 `5b703d76f`（去掉「品味」入口）推了上来，HEAD 在我脚下动了两笔。
工作区的文件跟着变了 —— 这不是看错，是真的变了。后果具体到一条：
那笔提交把首页 `const ERAS=[…]` 的末行和 `];` 并成了一行，而
`build_en.write_home` 的判据写的是 `const ERAS=\[.*?\n\];` ——
终止符写死成「换行 + 中括号」，一并行就一路吃到下一个 `\n];`
（DQ_DROP 的结尾），把中间的 `HW_SLUGS`、`DQX` 一起吞掉，
下一步替换 `HW_SLUGS` 报「没找到」，构建链停在第 6 步。
判据已改成按分号断（数组里不会出现分号）。
**写这类替换时，别把终止符落在别人随时会顺手改的换行上。**

**② 跑完构建要 `git diff` 一眼生成物，看有没有把别人手改的东西写回去。**
同一笔提交手改了 `robots.txt`，删掉两行 `/skill/` 的 sitemap 声明，
但没改**写它的那个脚本**（`seo/build_seo.py` 的 `extra_sitemaps`）。
于是当天的日更一构建，两行原样回来了 —— 如果照常提交，等于把别人
刚做完的事默默撤销，而十九道闸一条都不会响：产物和脚本是一致的，
只是脚本里那份已经过期。第零节「生成物永远别手改」是对写的人说的；
这一条是对**下一个跑构建的人**说的：**你的构建会把别人的手改抹掉，
而门禁看不出来。**

**③ 门禁自检跑着的时候不要动仓库。** `gate_selftest.py` 是往文件里注入
缺陷再还原，我在它跑的时候单独跑了一次 `seo/build_seo.py`，
它当场报了 4 条红（窄屏 2 条 + 工作区脏 2 条）。串行重跑：54 条全绿。
**自检红了先问一句：刚才有没有别的东西在写这个仓。**
